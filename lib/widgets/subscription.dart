//packages
import 'package:flutter/material.dart';

class ManageSubs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 400,
      width: 400,
      color: Colors.white,
    );
  }
}
